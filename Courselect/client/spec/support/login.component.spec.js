//import Login from '../../src/component/login.component';
import { sum } from '../../src/math';

describe('Login test cases', () => {

   it('check host url', () => {

   });


   it('check1', () => {

   })

   it('check2', () => {

   })

});


describe('Examining the syntax of Jest tests', () => {
   
   it('sums numbers', () => {
       expect(sum(1, 2)).toEqual(3);
       expect(sum(2, 2)).toEqual(4);
    });
 });